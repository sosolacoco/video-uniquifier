# Прогресс-бар (для консольного режима и логики).
tqdm>=4.66

# Pillow нужен ТОЛЬКО для генерации фейк-точек (generate_dots.py / кнопка в GUI).
# Сама уникализация видео работает и без него.
Pillow>=10

# Всё остальное — стандартная библиотека Python.
# Уникализация выполняется через системный FFmpeg (ffmpeg/ffprobe).
